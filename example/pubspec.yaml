name: mcp_example
description: Example usage of mcp_annotations and mcp_generator
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  easy_mcp_annotations:
    path: ../packages/easy_mcp_annotations
  dart_mcp: ^0.5.0
  shelf: ^1.4.0
  stream_channel: ^2.1.0

dev_dependencies:
  build_runner: any
  easy_mcp_generator:
    path: ../packages/easy_mcp_generator
