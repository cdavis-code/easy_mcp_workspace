# Example

See the [example](../../example) directory in the workspace root for a complete working example that demonstrates usage of both `easy_mcp_annotations` and `easy_mcp_generator` packages together.
