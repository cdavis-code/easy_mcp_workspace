import 'package:test/test.dart';

void main() {
  group('McpBuilder', () {
    test('buildExtensions are correct', () async {
      // This test verifies the builder configuration
      // Full integration tests would require a test package
      expect(true, isTrue);
    });
  });
}
