name: mcp_generator
description: Build Runner generator that creates MCP server code from @tool annotations.
version: 0.1.2
homepage: https://github.com/chrisdavis/easy_mcp
repository: https://github.com/chrisdavis/easy_mcp
issue_tracker: https://github.com/chrisdavis/easy_mcp/issues
environment:
  sdk: ^3.9.0
resolution: workspace
dependencies:
  analyzer: ">=6.0.0 <13.0.0"
  build: ^2.4.1
  source_gen: ^2.0.0
  code_builder: ^4.6.0
  # mcp_annotations:
  #   path: ../mcp_annotations
  json_annotation: ^4.9.0
  shelf: ^1.4.0

dev_dependencies:
  build_runner: ^2.4.0
  test: ^1.25.0
  lints: ^5.0.0

topics:
  - mcp
  - code-generation
  - build-runner
  - model-context-protocol
  - annotations

funding:
  - https://buymeacoffee.com/cdavis
